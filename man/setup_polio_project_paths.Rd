% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/polio_project_setup.R
\name{setup_polio_project_paths}
\alias{setup_polio_project_paths}
\title{Setup Polio Project Paths}
\usage{
setup_polio_project_paths(base_path = NULL, quiet = FALSE)
}
\arguments{
\item{base_path}{Character path to project root. If NULL (default), tries
to auto-detect the root directory.}

\item{quiet}{Logical; if TRUE, suppresses warnings about missing folders.
Default FALSE.}
}
\value{
Named list of absolute paths (see Path Keys section).
}
\description{
Creates a named list of standardized paths for polio pipelines. Call this
at the start of each analysis script to get consistent path references.
Detects project root automatically and constructs all data/output paths
with short, memorable names. Validates that folders exist and warns if not.
}
\section{Root Detection}{

When \code{base_path} is NULL, the function tries to detect the project root:
\enumerate{
\item Uses \code{here::here()} if the \code{here} package is available
\item Falls back to \code{rprojroot} heuristics (looks for \code{.here} file or git root)
\item Final fallback: current working directory
}
}

\section{Path Keys}{

The returned list contains these named paths:

\strong{Foundational data (1.1):}
\describe{
\item{\code{core}}{Project root directory}
\item{\code{admin_shp}}{Administrative boundary shapefiles}
\item{\code{physical_feat}}{Physical features (rivers, roads, etc.)}
\item{\code{settlements}}{Master list of settlements}
\item{\code{pop_national}}{National census population data}
\item{\code{pop_worldpop}}{WorldPop population rasters}
\item{\code{cache}}{Cached intermediate files}
}

\strong{Surveillance data (1.2):}
\describe{
\item{\code{es}}{Environmental surveillance data}
\item{\code{afp}}{Acute flaccid paralysis case data}
\item{\code{esurv}}{E-surveillance data}
}

\strong{Outbreak response (1.3-1.4):}
\describe{
\item{\code{outbreak}}{Outbreak response data}
\item{\code{orq_pre}}{Pre-campaign quality assessments}
\item{\code{orq_intra}}{Intra-campaign monitoring data}
\item{\code{orq_post}}{Post-campaign evaluation data}
}

\strong{Health systems & final (1.5-1.6):}
\describe{
\item{\code{dhs}}{DHS survey data}
\item{\code{final_data}}{Final analysis-ready datasets}
}

\strong{Outputs (03_outputs):}
\describe{
\item{\code{val}, \code{val_fig}, \code{val_tbl}}{Validation outputs}
\item{\code{interm}, \code{interm_fig}, \code{interm_tbl}}{Intermediate products}
\item{\code{final}, \code{final_fig}, \code{final_tbl}, \code{final_ppt}}{Final outputs}
}
}

\section{Side Effects}{

Sets the global option \code{polio.paths} to the returned list for easy retrieval
via \code{getOption("polio.paths")}.
}

\examples{
# typical usage at start of script
tmp <- tempdir()
initialize_polio_project(tmp)
paths <- setup_polio_project_paths(base_path = tmp, quiet = TRUE)

# access paths by name
paths$admin_shp
paths$es
paths$final_ppt

# build file paths
fs::path(paths$afp, "raw", "afp_cases_2024.csv")

# retrieve later via option
getOption("polio.paths")$cache
}
